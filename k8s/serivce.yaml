apiVersion: v1
kind: Service
metadata:
  name:  uci-heart-health-practice
spec:
  selector:
    app: heart-health-api-ci
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000